---
import { toggleMenu } from "../scripts/menu.js";
import { getLangFromUrl, useTranslations, useTranslatedPath } from "../i18n/utils.js";
import LanguagePicker from "./molecules/LanguagePicker.astro";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const translatePath = useTranslatedPath(lang);

---

<div
  class="nav-links hidden opacity-0 flex fixed w-full h-0 gap-2 text-2xl z-50 flex-col max-w-screen-lg text-[#fff] px-6 py-10 transform -translate-x-1/2 left-1/2 backdrop-blur-lg bg-[#111]
  md:w-auto md:top-4 md:flex md:flex-row md:rounded-full md:p-2 md:h-auto md:justify-between md:bg-[rgba(250,250,250,0.05)] md:text-base md:text-[#aaa] md:font-medium md:opacity-1 md:h-auto"
  style="text-shadow: 0px 0px 4px rgba(0,0,0,0.6);"
>
  <a
    href="#heroSection"
    class="nav-item md:bg-[rgba(250,250,250,0.1)] px-8 py-2 rounded-full"
    aria-label={t("nav.home")}
  >
    {t("nav.home")}</a
  >
  <a
    href="#projectSection"
    class="nav-item px-8 py-2 rounded-full flex"
    aria-label={t("nav.projects")}
  >
    {t("nav.projects")}</a
  >
  <!--a href="#serviceSection" class="nav-item px-8 py-2 rounded-full flex" aria-label="Servicios">Servicios</a-->
  <a
    href="#myproccessSection"
    class="nav-item px-8 py-2 rounded-full flex"
    aria-label={t("nav.designProcess")}
  >
    {t("nav.designProcess")}</a
  >
  <a
    href="#aboutSection"
    class="nav-item px-8 py-2 rounded-full flex"
    aria-label={t("nav.about")}
  >
    {t("nav.about")}</a
  >
  <a
    href="#pricingSection"
    class="nav-item px-8 py-2 rounded-full flex"
    aria-label={t("nav.price")}
  >
    {t("nav.price")}</a
  >
  <a
    href="#contactSection"
    class="nav-item px-8 py-2 rounded-full flex"
    aria-label={t("nav.contact")}
  >
    {t("nav.contact")}</a
  >
  <div class="nav-item"><LanguagePicker /></div>
  <!--a href="/about/" class="nav-item px-8 py-2 rounded-full flex">Sobre mi</a>
  <a href="/blog/" class="nav-item px-8 py-2 rounded-full flex">Blog</a>
  <a href="/tags/" class="nav-item px-8 py-2 rounded-full flex">Etiquetas</a-->
</div>
<div
  class="nav-links-desktop glass-effect hidden items-center fixed w-full gap-2 text-2xl z-50 flex-col max-w-screen-lg text-[#ddd] px-6 py-10 transform -translate-x-1/2 left-1/2 backdrop-blur-2xl shadow-xl bg-[#111]
   md:top-0 md:flex md:flex-row md:rounded-full md:p-2 md:h-auto md:justify-between md:text-base md:text-[#ccc] md:font-light"
>
  <a
    href="#heroSection"
    class="nav-item-desktop px-8 py-2 rounded-full transition-all duration-300 ease-in-out"
    aria-label={t("nav.home")}
  >
    {t("nav.home")}</a
  >
  <a
    href="#projectSection"
    class="nav-item-desktop px-8 py-2 rounded-full flex transition-all duration-300 ease-in"
    aria-label={t("nav.projects")}
  >
    {t("nav.projects")}</a
  >
  <!--a href="#serviceSection" class="nav-item-desktop px-8 py-2 rounded-full flex">Servicios</a-->
  <a
    href="#myproccessSection"
    class="nav-item-desktop px-8 py-2 rounded-full flex transition-all duration-300 ease-in"
    aria-label={t("nav.designProcess")}
  >
    {t("nav.designProcess")}</a
  >
  <a
    href="#aboutSection"
    class="nav-item-desktop px-8 py-2 rounded-full flex transition-all duration-300 ease-in"
    aria-label={t("nav.about")}
  >
    {t("nav.about")}</a
  >
  <a
    href="#pricingSection"
    class="nav-item-desktop px-8 py-2 rounded-full flex transition-all duration-300 ease-in"
    aria-label={t("nav.price")}
  >
    {t("nav.price")}</a
  >
  <a
    href="#contactSection"
    class="nav-item-desktop px-8 py-2 rounded-full flex transition-all duration-300 ease-in"
    aria-label={t("nav.contact")}
  >
    {t("nav.contact")}</a
  >
  <LanguagePicker />
  <!--a href="/about/" class="nav-item px-8 py-2 rounded-full flex">Sobre mi</a>
  <a href="/blog/" class="nav-item px-8 py-2 rounded-full flex">Blog</a>
  <a href="/tags/" class="nav-item px-8 py-2 rounded-full flex">Etiquetas</a-->
</div>

<script>
  import gsap from "gsap";
</script>
